{
  "name": "@chroxy/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Chroxy desktop tray app (Tauri)",
  "scripts": {
    "dev": "cargo tauri dev",
    "build": "cargo tauri build",
    "check": "cd src-tauri && cargo check"
  }
}
