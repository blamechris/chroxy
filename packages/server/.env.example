# Server port for WebSocket
PORT=8765

# tmux session name to attach to / create
TMUX_SESSION=claude-code

# Shell to spawn if no tmux session exists
SHELL_CMD=/bin/zsh

# Auth token clients must provide to connect
# Generate one: node -e "console.log(crypto.randomUUID())"
API_TOKEN=change-me-to-a-real-token
